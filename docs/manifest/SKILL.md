---
name: manifest
description: "MANIFEST_IO file-first execution contract. Use when: running any tool/script and you need reproducible artifacts. Triggers: manifest, manifest io, file-first, strict mode, MANIFEST_IO."
version: 0.1.0
---

# manifest

MANIFEST_IO is the default file-first execution workflow.
It standardizes inputs (JSON files), outputs (files + manifest), and stdout (short summary).

## References

- [MANIFEST_IO Details](references/manifest-io.md)
