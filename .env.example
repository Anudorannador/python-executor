# Example environment configuration for pyx (python-executor)
#
# Copy this file to the user config directory:
#   Windows: %APPDATA%\pyx\.env
#   Unix/macOS: ~/.config/pyx/.env
#
# You can also place a .env file in the current working directory
# to override user config values.

# pyx Configuration
#
# `pyx gs` is interactive and does not require env configuration.
# `pyx run` / `pyx python` may use UV proxy/index settings below.

# UV proxy and index settings (used by `pyx add` and `pyx python`)
# These are mapped to standard UV/pip environment variables
# PYX_UV_HTTP_PROXY=http://<proxy_host>:<proxy_port>
# PYX_UV_HTTPS_PROXY=http://<proxy_host>:<proxy_port>
# PYX_UV_NO_PROXY=localhost,127.0.0.1
# PYX_UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple

# -----------------------------------------------------------------------------
# Runtime-only variables (DO NOT set these in .env)
# -----------------------------------------------------------------------------
# These are automatically set by `pyx run` for the executed script.
# They describe the current run and output locations.
#
# PYX_INPUT_PATH   - Optional input JSON path
# PYX_OUTPUT_DIR   - Output directory for this run
# PYX_OUTPUT_PATH  - Manifest path for this run
# PYX_LOG_PATH     - Log file path for this run
# PYX_RUN_ID       - Unique run identifier

# Database connections
# MYSQL_URL=mysql+pymysql://user:password@host:3306/database
# REDIS_URL=redis://:password@host:6379/0

# API keys (examples)
# OPENAI_API_KEY=sk-xxx
# ANTHROPIC_API_KEY=sk-ant-xxx

# Custom configurations
# Add your own environment variables below
